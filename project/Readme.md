Скачать:
- pyQt
- openCV
- open3d
- matplotlib (вроде)
(получше должно быть в записке описано)

Какая штука. Просто отвратительная. {pyQt, openCV, open3d} - эта троица (а именно их окошки) не дружит. От слова совсем. Код вылетает, даже не кидая исключения. Именно поэтому вместо потоков (да даже без без них, с помощью "псевдопараллельности", а такое пробовалось) используется следующая ~~параша~~ система: для работы с изображениями (как rgb, так и depth) и облаками запускаются подпроцессы, условно отдельно работающие с openCV и open3d соответственно. Для взаимодействия используются сигналы/сообщения   

Для ознакомления есть фоточки и облака, которые можно потыкать

По функционалу - маленько не полный (а именно в разделе обработки облаков), ибо я устала
